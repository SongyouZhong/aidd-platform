# Grafana Dashboard 自动配置
apiVersion: 1

providers:
  - name: 'AIDD Dashboards'
    orgId: 1
    folder: 'AIDD'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
      foldersFromFilesStructure: false
